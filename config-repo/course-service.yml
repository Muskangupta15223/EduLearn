server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/course_db
    username: root
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
---
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:mysql://mysql-db:3306/course_db
    username: root
    password: ${SPRING_DATASOURCE_PASSWORD}
